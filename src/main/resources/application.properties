quarkus.http.ssl-port=${APP_PORT:8080}
app.name=${APP_NAME:${HOSTNAME:unknown}}
enabled.liveness=${LIVENESS:true}
enabled.readiness=${READINESS:true}

quarkus.rest-client.service-client-api.url=${SERVICE_CLIENT_URL:localhost:8080}